#include "gameover.h"
#include "grille.h"
/*

int End::gameOver() {
	int i = 0;
	int temp = 0;
	int notOver = 0;
	for (int x = -1; x++; x < 4)
		for (int y = -1; y++; y < 4)
			if (i == grille[x][y])
				temp++;
	if (temp == 16)
		for (int x = -1; x++; x < 4)
			for (int y = -1; y++; y < 4)
				if (grille[x][y] == grille[x - 1][y] || grille[x][y - 1] || grille[x + 1][y] || grille[x][y + 1])
					int notOver = notOver + 1;
}
*/